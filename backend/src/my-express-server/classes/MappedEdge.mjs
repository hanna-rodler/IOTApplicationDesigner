export default class MappedEdge {
    constructor(mappingId, sourceTopicId, targetTopicId) {
        this.mappingId = mappingId;
        this.sourceTopicId = sourceTopicId,
        this.targetTopicId = targetTopicId
    }
}